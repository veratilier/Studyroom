{
  "name": "studyroom",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check docs/app.js && node --check docs/library.js && node --check docs/extract.mjs && node --check worker/index.mjs",
    "dev:api": "wrangler dev --config worker/wrangler.jsonc",
    "deploy:api": "wrangler deploy --config worker/wrangler.jsonc"
  }
}
